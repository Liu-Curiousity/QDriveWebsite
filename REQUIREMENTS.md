## 网站需求说明（初始版本）

**最近更新时间**：2026-03-18

### 1. 基本信息

- **公司类型 / 产品**：伺服电机及相关运动控制解决方案
- **网站用途**：
  - 企业宣传（品牌形象、产品/方案展示）
  - 技术博客（文章、技术分享）
  - 资料下载（本地存储的 PDF / 文档，走服务器带宽）
- **目标访问人群**：主要为国内 B2B 客户（机床、机器人、物流等高端装备领域）

### 2. 技术路线

- **整体架构**：静态网站
- **框架**：Astro（当前使用 Astro 6.x）
- **内容形式**：
  - Markdown（博客、部分文案）
  - 图片素材（产品图、场景图、示意图等）
- **部署环境**：
  - 服务器：Linux（使用 Nginx 等方式部署静态资源）
  - 服务器性能与带宽：一般，不追求复杂后端逻辑
- **前端要求**：
  - 支持 PC 与移动端（自适应布局）
  - 不需要用户登录、注册，不需要在线支付等复杂交互

### 3. 设计与视觉风格

- **主题色**：黑 + 白，为主色；整体为黑白灰冷色科技感风格
- **彩色使用**：尽量克制，彩色仅用于必要的对比/提示（如轻微高亮），避免大面积彩色背景
- **模式支持**：
  - 支持浅色模式和深色模式
  - 能够根据系统设置（`prefers-color-scheme`）自动切换
- **整体调性**：
  - 冷静、理性、科技感
  - 偏工业/高端装备领域风格，而非活泼互联网风

- **全站统一动效与进入体验**：
  - 采用与首页一致的“进入动画”风格：`淡入 + 轻微上移`，通过 `IntersectionObserver` 在元素进入视口后触发；同时兼容 `prefers-reduced-motion`（减少动效时直接展示、不做动画）。
  - 全站主要内容区域在适当位置使用同款进入动画，保证整体观感统一。

- **全站统一导航栏（与首页完全一致）**：
  - 其它页面必须复用与首页相同的顶部导航栏结构、文案、布局与悬浮弹窗交互（包含“产品中心”的悬浮菜单/弹窗、同款间距与样式、同款浅色/深色适配）。
  - 导航栏应保持与首页一致的 HTML/CSS 逻辑与交互时机，避免出现视觉或交互差异。

### 4. 功能与页面结构（当前规划层面）

- **首页**
  - 强调伺服电机 / 运动控制的核心卖点
  - 展示典型应用场景：精密机床、工业机器人、物流分拣、定制方案等
  - 图片素材规划：
    - QD4310 白底渲染图（含爆炸图）
    - 二维云台模型图
    - 适量应用场景示意图（可先用网络示意图，占位，后续替换为自有素材）
    - 公司类图片暂缺，后续补充团队/产线/办公环境等
  - 顶部导航包含：
    - 产品中心
    - 解决方案 / 应用场景
    - 技术博客
    - 资料下载中心
- **产品中心（入口 + 独立产品页）**
  - 当前已知产品（每个产品拥有独立页面）：
    - 电机：QD4310 伺服电机平台（如 `/products/qd4310`）
  - 产品结构规划：
    - 顶层按「电机」与「衍生产品」分类
    - 电机类预留不同形态：云台电机、关节电机等
    - 衍生产品类预留：云台、关节模组、定制整机等
  - 页面架构要求：
    - 首页不堆叠过多具体产品细节，只负责引导到各产品独立页面
    - 每个产品页面应包含：产品介绍、参数/特点、应用场景、该产品相关资料下载区域
  - 导航交互（产品中心悬浮窗）：
    - 触发方式：鼠标悬停或点击导航中的“产品中心”入口
    - 弹窗内容：仅展示产品类别 / 名称列表或简单分组，不在弹窗中写详细功能描述
    - 视觉风格：与整站黑白灰风格保持一致
- **解决方案**
  - QGimbal 二维云台解决方案页面（如 `/solutions/qgimbal`）
  - QCar 二轮平衡车解决方案页面（如 `/solutions/qcar`）
  - 后续可增加更多行业/系统级解决方案页面
  - 首页顶部轮播展示当前产品/方案卡片，并通过指示点显示当前所在卡片序号
- **技术博客**
  - 提供与伺服控制、选型经验、应用案例等相关的技术文章
  - 文章内容以 Markdown 形式维护
  - 具有独立页面（如 `/blog`），首页只作为入口
- **关于 QDrive / 资料中心**
  - 独立页面（如 `/about`）展示公司与产品线概览
  - 资料下载内容存储在本地服务器（静态目录），例如 `public/downloads/...`
  - 页面提供下载列表（标题、简介、下载链接）
- **其他页面（后续细化）**
  - 关于我们 / 公司介绍
  - 产品与解决方案详情
  - 联系方式、技术支持等

### 4.1 网页上位机波形

- 电机网页上位机在中间工作区提供「终端 / 波形」切换，串口连接与两侧控制、配置功能保持可用。
- 波形模式按用户设置的频率发送 `status`，解析英文冒号分隔的 `Current:`、`Speed:`、`Angle:` 返回行，分别绘制电流、速度、角度曲线。
- 采样收发不写入终端，避免轮询造成终端输出堆积；手动命令与终端交互仍保留原有显示。
- 支持开始/停止采样、30、60、90、120 Hz 四档频率选择与采样状态显示；停止后再次开始时沿用已有曲线继续采样，停止期间不在时间轴中保留空档；Shift + 滚轮缩放纵轴，Ctrl + 滚轮缩放时间轴，无修饰滚轮或鼠标拖拽平移时间轴，双击图表恢复自适应范围。横轴缩放与自适应视窗最多显示 10 秒；单击曲线可将当前位置设为横轴 `0 s`；纵轴采用自动易读刻度，鼠标悬停时三图同步显示时间游标、图内对应数值与横轴时间标记。
- 状态帧中电流、速度、角度、末尾电压字段及随后的命令行提示符完整到达后立即结束该次采样，状态文本不写入终端；图形渲染按动画帧合并并按像素抽稀，以支持 100 Hz 及以上的虚拟串口采样；采样时禁用终端直接输入。
- 用户通过控制、配置等界面操作发送命令后，轮询暂停 3 个当前采样周期（该参数后续可调），确保手动命令优先。

### 4.2 通用串口助手

- 网页上位机工具页提供不绑定具体产品协议的「通用串口助手」，通过浏览器 Web Serial API 选择并连接任意串口设备。
- 支持自定义波特率，以及数据位、停止位、校验位和流控制配置；连接后可控制 DTR、RTS，并读取 CTS、DSR、DCD、RI 线路状态。
- 支持文本与 HEX 两种收发模式、CR / LF / CRLF 行尾、发送历史、循环发送、可持久化的快捷指令；循环发送间隔最小为 10 ms，并按单调时钟的固定目标时刻调度，扣除串口写入与界面更新耗时，避免周期累积漂移；在循环间隔或发送次数输入框中按 Enter 仅确认输入，不触发发送。
- HEX 发送模式可在帧尾追加奇偶校验、异或校验、低 8 位累加和或 CRC-8/ATM 校验字节，并在校验选择栏旁实时显示结果；奇偶校验统计整帧置位数，奇数追加 `0x01`、偶数追加 `0x00`，CRC-8/ATM 使用 Poly `0x07`、Init `0x00`、RefIn / RefOut `false`、XorOut `0x00`。
- 数据监视器支持收发方向、毫秒时间戳、本地发送回显、收发字节计数、清空和 CSV 日志导出；日志位于底部时自动跟随新数据，用户向上滚动后保持当前位置；每次串口读取的原始 RX 数据块及到达时间始终独立保存，仅在显示与导出时动态分组：时间戳开启时，相邻且位于同一毫秒的 RX 块显示为一条；时间戳关闭时，到达间隔不超过 3 ms 的相邻 RX 块持续合并显示，切换时间戳后对已接收日志立即重新分组；高频收发时日志按浏览器动画帧批量刷新，避免逐块分行和布局计算阻塞串口调度。
- 串口数据仅在浏览器本机处理；页面兼容浅色 / 深色模式和移动端布局，并在不支持 Web Serial 的浏览器中给出明确提示。

### 4.3 USB 转 CAN 上位机

- 网页上位机工具页提供基于 WebUSB API 的「USB 转 CAN 上位机」，直接连接采用 `gs_usb` 协议的 USB-CAN 设备；首次通过“选择并连接”选择设备，主动断开后“断开”按钮切换为“重连”，可直接重新打开上一次选择的设备。
- 连接设备后读取通道数与通道位时序约束，并立即按当前通道、波特率和工作模式启动 CAN；默认波特率为 1 Mbps，波特率菜单始终完整显示全部预设值且不使用内部滚动条。用户重新选择任一配置后，页面自动执行 RESET、应用位时序与工作模式并重新 START，无需额外的 CAN 启停按钮，也无需断开 USB。根据设备 CAN 时钟计算所选波特率的 BRP、TSEG 与采样点；支持正常、仅监听和回环模式，以及设备固件声明支持时的软件 120 Ω 终端电阻控制；连接后不额外展示设备名称、VID / PID、固件 / 硬件版本或实际位速率详情。
- 首版面向经典 CAN，支持标准帧、扩展帧、RTR 帧、用户直接输入的 0–8 DLC、0–8 字节十六进制数据，以及接收错误帧统计；DLC 为严格的单字符输入，已有数字时必须先删除才能输入新值。ID 仅接受 `0–9`、`A–F` 并自动转换为大写，数据输入复用通用串口助手 HEX 模式的输入、粘贴过滤、自动大写与快捷整理逻辑，允许空格和连续字符混输。普通帧数据不足 DLC 时补 `00`、超出时截断，RTR 帧按所填 DLC 发送且不携带数据。发送区复用通用串口助手的发送方式菜单和分段按钮交互，循环发送支持 10–86400000 ms 间隔、1–100000 次有限发送或 `-1` 无限发送，并按单调时钟的固定目标时刻调度以避免周期累积漂移；循环状态显示在扩展帧、远程帧选项右侧，在循环间隔或发送次数输入框中按 Enter 仅确认输入，不触发发送。
- 帧监视器支持按 ID / 数据、收发方向和帧类型筛选，方向使用单选下拉，帧类型使用可多选的弹出复选面板（标准帧、扩展帧、RTR 帧、错误帧），支持收发统计、清空及 CSV 日志导出；日志位于底部时自动跟随新帧，用户向上滚动后保持当前位置；CAN 数据仅在浏览器本机处理。
- 四个网页上位机（Serial、CAN、Motor、Gimbal）统一复用网页上位机基础控件样式、下拉栏交互与 `--web-host-control-height` 控件高度变量；Tooltip 行为与样式也使用共享资源，新增上位机页面不得重新硬编码不同高度或重复实现基础下拉栏。
- 页面兼容浅色 / 深色模式和移动端布局，并在不支持 WebUSB 的浏览器中给出明确提示；Windows 下提示 USB 接口需要 WinUSB 驱动。

### 5. 非功能性需求

- **性能**：
  - 优先考虑静态资源 + 浏览器端渲染，减少服务器计算压力
  - 图片应进行合理压缩与格式优化（如 WebP 等）
- **SEO**：
  - 面向中文搜索引擎，适当设置 `title`、`meta description`
- **维护性**：
  - 文案和文章更新应尽量通过 Markdown 或少量配置修改完成
  - 重要结构和样式变更由你（AI 助手）在代码层面协助完成

---

> 说明：  
> 本文档用于记录和追踪网站需求。  
> **后续每次需求变更时，需要同步更新本文件**，保持与实际实现一致。请在修改代码或结构的同时，补充/修正相应条目。
